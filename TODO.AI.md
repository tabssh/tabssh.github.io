# TODO.AI.md

- docs/features/index.html: page title/nav calls it "TabSSH Features" (platform-neutral)
  but the entire body ("Enterprise-grade SSH functionality designed for Android devices")
  and every feature section is Android-only. Desktop and Web now have their own
  install/usage docs but no equivalent full feature deep-dive. Either scope the title to
  "Android Features" or expand the page to cover Desktop + Web feature parity/differences.
- SPEC.md at repo root is stale — describes a Jekyll build and Android-only scope; the
  site is actually plain static HTML/CSS/JS (no Jekyll) and now covers Android, Desktop,
  and Web. Needs a rewrite to match reality.
